<Page x:Class="UnoTest.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:UnoTest"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
  <!--DataContext="{Binding MainPageViewModelInstance, Source={StaticResource Locator}}">-->

  <Grid Background="LightGray" >

    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition />
    </Grid.RowDefinitions>

    <Grid.ColumnDefinitions>
      <ColumnDefinition />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>

    <Button
      Content="Click to get web data using HttpClient..."
      Command="{Binding GetDataUsingHttpClientCommand}"
      HorizontalAlignment="Center"
      FontSize="20"
      Foreground="White"
      Background="Navy"/>

    <Button
      Grid.Column="1"
      Content="Click to get web data using BreezeSharp..."
      Command="{Binding GetDataUsingBreezeSharpCommand}"
      HorizontalAlignment="Center"
      FontSize="20"
      Foreground="White"
      Background="Navy"/>

    <TextBlock
      Grid.Row="1"
      Grid.ColumnSpan="2"
      HorizontalAlignment="Stretch"
      Text ="Result:"
      FontSize="20"
      Foreground="DarkRed"
      FontWeight="Bold" />

    <TextBlock
      Grid.Row="2"
      Grid.ColumnSpan="1"
      HorizontalAlignment="Stretch"
      VerticalAlignment="Stretch"
      Text ="{Binding Result}"
      FontSize="12"
      Foreground="DarkRed" />    
    
       
    <ListView
      Grid.Row="2"
      Grid.Column="2"
      ItemsSource="{Binding CustomersFromBreeze}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <StackPanel Orientation="Horizontal">
            <TextBlock Background="Red" Text="{Binding FirstName}" />
            <TextBlock Background="Yellow" Text="{Binding LastName}" />
            <TextBlock Background="Orange" Text="{Binding Description}" />            
          </StackPanel>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>

  </Grid>



  <!--<Grid  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>

    <ListView Background="Silver"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch"
               ItemsSource="{Binding NavigationItems}"
              SelectedItem="{Binding SelectedNavigationItem, Mode=TwoWay}" >
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextBlock Text="{Binding}"
             Foreground="White"
             FontSize="15"
             FontWeight="Bold"
             Margin="5"/>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>

    <Grid Grid.Column="1">
      <Grid.RowDefinitions>
        <RowDefinition />
        <RowDefinition />
        <RowDefinition />
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition />
      </Grid.ColumnDefinitions>

      <Grid Background="LightBlue">
        <Grid.RowDefinitions>
          <RowDefinition/>
          <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="TextBlock"
                   Margin="20"
                   FontSize="50"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Text="{Binding NumberOfClicks}" />

        <TextBlock Grid.Row="1" x:Name="CustomerNameTextBlock"
                     Margin="20"
                     FontSize="50"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Center"
                     Text="{Binding CustomerName}" />
      </Grid>

      <Button Grid.Row="1"
              Grid.ColumnSpan="2"
              FontSize="35"
              Foreground="White"
              Background="Green"
              Content="Click Me !"
              VerticalAlignment="Center"
              HorizontalAlignment="Center"
              Command="{Binding ClickMeButtonCommand}" />

      <ListView Grid.Row="0"
                Grid.Column="1"
                Background="Blue"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                ItemsSource="{Binding Numbers}">
        <ListView.ItemTemplate>
          <DataTemplate>
            <TextBlock Text="{Binding}"
                       Foreground="White"
                       FontSize="20"
                       FontWeight="Bold" />
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>

      <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Vertical">
        <TextBlock HorizontalAlignment="Left"
                  VerticalAlignment="Top"
                  Text ="Error Message"
                  FontSize="13"
                  Foreground="Red"
                   FontWeight="Bold"/>
        <TextBlock HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Text ="{Binding ErrorMessage}"
                    FontSize="11"
                    Foreground="Red" />
      </StackPanel>
    </Grid>
  </Grid>-->
</Page>
